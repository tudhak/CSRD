// Set base URL for API calls and default headers

const API_BASE_URL = "http://localhost:3000";

const defaultHeaders = {
  "Content-Type": "application/json",
};

export { API_BASE_URL, defaultHeaders };
